<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.codexperiments.leakeeper.test">
    <application>
        <!-- For tests only. It doesn't seem possible to declare test activities in androidTest directory -->
        <activity android:name=".TestActivity"/>
        <activity android:name="com.codexperiments.leakeeper.test.task.helper.TaskActivity" />
    </application>
</manifest>
